<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>
      t('mailer.hello') <%= @user.full_name %>
        t('mailer.The_f') t('mailer.donation') t('mailer.for') <%= link_to @wish_item.title, organization_wish_item_path(@wish_item.organization.id, @wish_item.id) %> t('mailer.was_canceled_f').
        <%= @organization.name %> t('mailer.will_be_notified').
      <p>t('mailer.have_great_day')</p>
    </p>
  </body>
</html>