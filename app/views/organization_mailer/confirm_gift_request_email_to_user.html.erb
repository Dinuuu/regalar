<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>
      <%= t('mailer.hello') %> <%= @user.full_name %>
        <%= @organization.name %> <%= t('mailer.confirmed') %> <%= t('mailer.the_m') %> <%= t('mailer.gift_request_model') %> <%= t('mailer.for') %> <%= link_to @gift_item.title, user_gift_item_path(@gift_item.user.id, @gift_item.id) %>.
        <%= t('mailer.you_can_contact') %> <%= @organization.name %> <%= t('mailer.writing_to') %> <%= @organization.email %>
      <p> <%= t('mailer.have_great_day')%></p>
    </p>
  </body>
</html>