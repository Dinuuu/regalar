<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>
      <%= t('mailer.hello') %> <%= @organization.name %>
        <%= t('mailer.The_m') %> <%= t('mailer.gift_request_model') %> <%= t('mailer.for') %> <%= link_to @gift_item.title, user_gift_item_url(@gift_item.user.slug, @gift_item.slug) %> <%= t('mailer.was_created_m') %>
        <%= @user.full_name %> <%= t('mailer.will_be_notified') %>.
      <p> <%= t('mailer.have_great_day')%></p>
    </p>
  </body>
</html>